rules_version = '2';
// Test-unit emulator only: wide open. Not used in production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
