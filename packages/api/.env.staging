# Emulator Environment Variables

# Current environment identifier
CURRENT_ENV=staging

# The API_EXTERNAL_BASE_URL currently is being used only in local development and testing environments, so we don't 
# need it here for now.
