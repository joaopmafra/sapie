# Web package documentation

Firebase (client), env vars, and how the SPA interacts with the API for note bodies.

| Document | Purpose |
|----------|---------|
| [firebase-web.md](./firebase-web.md) | Firebase Auth setup, web env vars, emulators, OAuth; signed URL loads and **when to fix bucket CORS** (link to API doc) |
