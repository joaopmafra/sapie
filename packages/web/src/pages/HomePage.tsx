import Workspace from '../components/Workspace';

/**
 * Home Page
 *
 * This page serves as the main entry point for authenticated users,
 * displaying their content workspace.
 */
const HomePage = () => {
  return <Workspace />;
};

export default HomePage;
